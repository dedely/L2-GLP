package cli.process.output;

import cli.data.map.Map;

public class MapOutput {
	Map map;

	public MapOutput(Map map) {
		this.map = map;
	}

	/*
	 * public void output() { int width, height;
	 * 
	 * for (int i = 0; i < height; i++) { for (int j = 0; j < width; j++) { tmp +=
	 * tiles[i][j] + " "; } tmp += "\n"; }
	 */

}